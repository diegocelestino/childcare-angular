import { MovimentMapPipe } from './moviment-map.pipe';

describe('MovimentMapPipe', () => {
  it('create an instance', () => {
    const pipe = new MovimentMapPipe();
    expect(pipe).toBeTruthy();
  });
});
