import { ObservationTypePipe } from './observation-type.pipe';

describe('ObservationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ObservationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
